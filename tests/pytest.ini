[pytest]
# auto: las funciones `async def test_*` se ejecutan sin decorar cada una.
asyncio_mode = auto
# Cada test tiene su propio event loop, incluidos sus fixtures (aislamiento total entre pruebas).
asyncio_default_fixture_loop_scope = function
